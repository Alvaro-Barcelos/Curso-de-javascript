let valorMaior = max(99.9, 99.8);
console.log(valorMaior);

function max(num1, num2) {
     if (num1 > num2 )
        return num1;
     else return num2;

    //  if (num1 > num2 )
    //      return num1;
    //  return num2;

    //  return num1 > num2 ? num1: num2;
}